spring:
  application:
    name: LinkSnap

  datasource:
    url: jdbc:postgresql://localhost:5432/linksnap
    username: postgres
    password: lALESWAR@28
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

linksnap:
  domain: http://localhost:8080